<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group
        android:id="@+id/menu_More">
        <item android:id="@+id/menuNewGroup"
            android:title="New group" />
        <item android:id="@+id/menuBroadcast"
            android:title="New Broadcast" />
        <item android:id="@+id/menuWeb"
            android:title="WhatsApp web" />
        <item android:id="@+id/menuMessage"
            android:title="Started Messages" />
        <item android:id="@+id/menuSetting"
            android:title="Settings" />
    </group>
</menu>
